range()函数
for value in range(1,5)
	print(value)

实际打印出来的是1\n2\n3\n4
但没有5，range()相当于左边闭区间，右边开区间


numbers=list(range(1,5))
使用list()函数把range()生成的数转化为列表


range()指定步长
range(3,11,2)
得到3,5,7,9


创建平方数列表与列表解析
1.
squares=[]
for value in range(1,11):
	square=value**2
	squares.append(square)
print(squares)

2.列表解析
squares = [value**2 for value in range[1,11)]
print(squares)





切片
players[-3:]表示从倒数第三个到列表最后
players[0:3]表示从第0个到第3个前面,也就是到第2个
或者理解为从开始往后数3-0=3个


复制列表
copiedfoods=foods[:]
用切片,从头切到尾,表示整个列表
如果直接copiedfoods=foods[:]   只是创建了一个索引,这两个名字都表示同一个东西,没有真的复制




4.5元组
和列表类似,但是元组的元素是不可更改的!
他的定义和列表的不同之处就是:用小括号()而不是方括号[] 
例如:
dimensions=(100,50)
===
但是
===
虽然元素不能改,也就是说,dimensions[0]=250 会被拒绝
但是可以重新给存储元组的变量赋值,也就是我可以dimensions=(250,50)
加一个元素-----dimensions=(250,50,20)-------原则上也可以,试试看




